{
  "_comment": "Install at ~/.codex/hooks.json (user) or .codex/hooks.json (project). Replace __HOOK_DIR__ with the absolute path to examples/coding-agent-guard/hooks. `install.sh codex` does this for you. Codex shares Claude Code's hook protocol, so it reuses anthropic-hook.sh.",
  "PreToolUse": [
    {
      "matcher": "^Bash$",
      "hooks": [
        {
          "type": "command",
          "command": "__HOOK_DIR__/anthropic-hook.sh",
          "timeout": 30,
          "statusMessage": "Tool Guard policy check"
        }
      ]
    }
  ]
}
