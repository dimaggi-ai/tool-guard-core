{
  "name": "operator_eq",
  "description": "The operator eq condition is evaluated through a loaded YAML policy and matches its dedicated rule.",
  "policy_file": "fixtures/operator_matrix.yaml",
  "mode": "enforcement",
  "envelope": {
    "tool_name": "operator_probe",
    "parameters": {
      "case": "op_eq",
      "value": "alpha"
    }
  },
  "expect": {
    "decision": "denied",
    "action_taken": "denied",
    "matched_rule_ids": [
      "op-eq"
    ]
  }
}
